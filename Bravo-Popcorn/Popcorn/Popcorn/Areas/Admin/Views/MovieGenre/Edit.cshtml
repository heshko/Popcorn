@model Popcorn.Models.MovieGenreModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>MovieGenreModel</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="MovieModelId" class="control-label"></label>
                <select asp-for="MovieModelId" class="form-control" asp-items="ViewBag.MovieModelId"></select>
                <span asp-validation-for="MovieModelId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="GenreModelId" class="control-label"></label>
                <select asp-for="GenreModelId" class="form-control" asp-items="ViewBag.GenreModelId"></select>
                <span asp-validation-for="GenreModelId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
